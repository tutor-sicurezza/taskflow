# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*-dist
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.local
.env.production
.env.*.local
.env.development
**/agent-eval-report*
packages
pids
.file-manifest
.devcontainer/

.spark-workbench-id

# Sensitive documentation and backups
WEBMASTER_GUIDE.md
EMAIL_CONFIG.md
*.backup.json
taskflow-backup-*.json
audit-logs/
backups/
smtp-credentials.json
database-config.json
secrets.json
*.pem
*.key
*.crt
.vercel

# Stato locale della Supabase CLI (project ref del progetto collegato):
# e' per macchina, non va condiviso.
supabase/.temp/

# Configurazione Supabase specifica dell'installazione: project ref, dominio e
# mittente. Qui e' gia' tracciata da prima e resta tale; la regola serve al
# repository pubblico, che riceve questo file dalla sincronizzazione e non deve
# mai accogliere il config.toml di nessuno. Si parte da config.example.toml.
supabase/config.toml
